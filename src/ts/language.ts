// en handle

let en = document.getElementById('en')!;
let css = 'color: var(--hoover-primary);text-decoration: none; pointer-events: none; display: inline-block;transform: skew(0, 8deg)';

en.style.cssText += css;
